topology = mesh;
k = 4;
n = 2;

// Routing
routing_function = dim_order;


router = iq;
// Flow control
num_vcs     = 1;
vc_buf_size = 10;

// Traffic
traffic = uniform;
packet_size = 1;

// Simulation
sim_type = throughput;

injection_rate = 0.01;
sample_period = 1000;
max_samples = 10;

// CSV output
print_csv_results = 1;
